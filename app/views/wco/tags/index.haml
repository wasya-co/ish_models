
.tags-index.padded
  .header
    %h5.title Tags (#{@tags.length})

  = render 'index_table', tags: @tags

  %hr

  .tags-new.maxwidth
    .header
      %h5.title New tag

    = render 'form', tag: @new_tag
